% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/metrics.R
\name{compute_rafe}
\alias{compute_rafe}
\title{Risk-Adjusted Forecast Error (RAFE)}
\usage{
compute_rafe(
  mu_hat,
  mu,
  Sigma = NULL,
  Sigma_inv = NULL,
  variant = c("none", "cc05", "cc0", "cv", "i")
)
}
\arguments{
\item{mu_hat}{Numeric vector of forecast means.}

\item{mu}{Numeric vector of realised means (same length as \code{mu_hat}).}

\item{Sigma}{Realised covariance matrix. The argument order matches the
paper's replication code, so \code{compute_rafe(mu_hat, mu, Sigma)} does the
same thing here as it does there.}

\item{Sigma_inv}{Optional precision matrix, supplied instead of \code{Sigma} when
it is already available (it avoids an inversion). Must be a \emph{precision},
not a covariance. Ignored unless \code{variant} is \code{"none"}.}

\item{variant}{Covariance restriction from the nested sequence of
Section 3.3; see \code{\link[=restrict_cov]{restrict_cov()}}. \code{"i"} returns the paper's RMSE.}
}
\value{
A length-1 numeric.
}
\description{
Computes \eqn{\mathrm{RAFE} = \sqrt{(\hat\mu - \mu)^\top \Sigma^{-1} (\hat\mu - \mu)}},
the risk-adjusted distance between a forecast mean and the realised mean.
}
\details{
The metric is a Mahalanobis distance in the population-risk metric, with no
\eqn{1/N} normalisation. Consequently, at \eqn{\Sigma = I} it reduces to the
Euclidean norm of the forecast error, which is \eqn{\sqrt{N}} times the
conventional RMSE — see the examples.
}
\examples{
set.seed(1)
mu     <- rnorm(5)
mu_hat <- mu + rnorm(5, sd = 0.1)

compute_rafe(mu_hat, mu, Sigma = diag(5))

# At Sigma = I the metric is sqrt(N) times RMSE:
sqrt(5) * sqrt(mean((mu_hat - mu)^2))

}
\references{
Salcher, L., Stöckl, S., & Hanke, M. (2026). Lost in Translation?
Risk-Adjusting RMSE for Economic Forecast Performance.
\emph{Journal of Forecasting}. doi:10.1002/for.70134
}
