% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/metrics.R
\name{restrict_cov}
\alias{restrict_cov}
\title{Restricted Covariance Matrices for the Nested Metric Sequence}
\usage{
restrict_cov(Sigma, variant = c("none", "cc05", "cc0", "cv", "i"))
}
\arguments{
\item{Sigma}{A covariance matrix.}

\item{variant}{One of \code{"none"}, \code{"cc05"}, \code{"cc0"}, \code{"cv"}, \code{"i"}.}
}
\value{
A covariance matrix of the same dimension as \code{Sigma}.
}
\description{
Applies the sequence of simplifying assumptions of Salcher, Stöckl & Hanke
(2026, Section 3.3) to a covariance matrix. Imposing them one after another
turns the risk-adjusted forecast error into the ordinary root mean squared
error, which is how the paper frames RMSE as a severely restricted special
case of RAFE.
}
\details{
\describe{
\item{\code{"none"}}{The covariance is left alone: the full RAFE.}
\item{\code{"cc05"}}{All pairwise correlations replaced by
\eqn{\rho = 0.5}, variances kept — Equation (17).}
\item{\code{"cc0"}}{All correlations set to zero, variances kept; the
covariance becomes diagonal.}
\item{\code{"cv"}}{Correlations zero and all variances set to the average
variance of \code{Sigma}.}
\item{\code{"i"}}{Correlations zero and all variances set to one, giving the
identity. With this restriction the mean error is the RMSE of the paper,
\eqn{\lVert\hat\mu - \mu\rVert_2}.}
}
}
\examples{
set.seed(1)
Sigma <- stats::cov(matrix(rnorm(200), 40, 5))
round(stats::cov2cor(restrict_cov(Sigma, "cc05")), 3)   # all rho = 0.5
restrict_cov(Sigma, "i")                                # identity

}
\references{
Salcher, L., Stöckl, S., & Hanke, M. (2026). Lost in Translation?
Risk-Adjusting RMSE for Economic Forecast Performance.
\emph{Journal of Forecasting}. doi:10.1002/for.70134
}
