% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estimators.R
\name{sigma_crafe_floor}
\alias{sigma_crafe_floor}
\title{C-RAFE-Tuned Eigenvalue Floor for Covariance}
\usage{
sigma_crafe_floor(Sigma_hat, tau_rel = NULL)
}
\arguments{
\item{Sigma_hat}{Forecast covariance.}

\item{tau_rel}{Relative floor, typically in \verb{[0, 0.5]}. A value of 0 returns
\code{Sigma_hat} symmetrised and unchanged.}
}
\value{
A cleaned covariance matrix (same dimension as \code{Sigma_hat}), with
the applied absolute floor attached as attribute \code{tau_abs}.
}
\description{
Cleans \code{Sigma_hat} by raising small eigenvalues to a floor set relative to
the mean eigenvalue: with \eqn{\hat\Sigma = Q\,\mathrm{diag}(\hat\lambda)\,Q^\top},
\deqn{\tilde\Sigma(\tau) = Q\,\mathrm{diag}\bigl(\max(\hat\lambda_i,\ \tau\,\bar\lambda)\bigr)\,Q^\top,}
where \eqn{\bar\lambda} is the mean of the sample eigenvalues. The relative
parameterisation makes \code{tau_rel} scale-free.
}
\details{
\code{tau_rel} is tuned on inner-validation C-RAFE by \code{\link[=sep_tune]{sep_tune()}} and
\code{\link[=joint_trafe_tune]{joint_trafe_tune()}}; this function applies a given floor.
}
\examples{
set.seed(1)
R <- matrix(rnorm(30 * 5), 30, 5)
Sigma_hat <- stats::cov(R)

eigen(Sigma_hat, only.values = TRUE)$values
eigen(sigma_crafe_floor(Sigma_hat, 0.3), only.values = TRUE)$values

}
\references{
Stöckl, S., Salcher, L., & Hanke, M. Post-Optimal Moment Correction for
Mean-Variance Portfolios. Working paper.
}
